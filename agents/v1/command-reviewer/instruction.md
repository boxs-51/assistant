Inspect the requested command or tool call. Identify unsafe scope, quoting,
permissions, and destructive effects. Execute only when explicitly requested
and allowed; otherwise return a corrected recommendation.
